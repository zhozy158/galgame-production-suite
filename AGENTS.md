# Galgame 制作工作区规则

本目录是多作品共用的制作套件，不是单一游戏。任何 Agent 在修改这里之前必须按顺序阅读：

1. `README.md`：目录职责、创建与校验命令。
2. `framework/README.md` 与 `framework/DATA_FORMAT.md`：公共引擎边界和剧情数据格式。
3. `art-direction/STYLE_GUIDE.md`：所有作品默认继承的基础画风与身份母版制度。
4. `docs/PRODUCTION_WORKFLOW.md`：从立项到发布的标准流程。
5. 目标作品自己的 `README.md`、`docs/` 和 `art/`。

## 架构边界

- 依赖方向只能是 `games/<id> → framework`；公共框架不得引用任何单部作品的剧情、角色或素材。
- 每部作品只能修改自己的 `game/`、`theme.css`、`assets/`、`art/` 和 `docs/`，除非任务明确要求新增跨作品能力。
- 新建作品必须使用 `npm run new-game -- <slug> "<标题>"`，不得复制某部已有游戏作为模板。
- 每部作品必须使用唯一的 `GAME_CONFIG.id` 和 `storagePrefix`。
- 通用能力应以数据节点或配置实现，禁止在公共引擎中加入按游戏 ID 判断的特例。

## 美术边界

- 每个角色必须先有经用户确认的身份母版；未确认图片只进入 `assets/concepts/`。
- 生成新立绘或 CG 时，必须使用目标作品角色圣经指定的母版作为身份参考，禁止只凭名字或旧提示词重新生成。
- 完整提示词、参考图角色、输出路径和验收状态必须写入目标作品的 `art/`，不能只保留在对话中。
- 表情差分优先编辑同一标准立绘，只改变眉眼和嘴部；不得重绘身体、发型或服装。
- 全局默认画风遵守 `art-direction/STYLE_GUIDE.md`，尤其禁止油亮皮肤、空气笔厚涂、3D 感和身份漂移。
- 不得直接覆盖已接入游戏的素材；先生成版本化候选，整套验收后统一切换配置。

## 验证要求

- 修改公共框架或模板后，在本目录运行 `npm test`。
- 修改单部剧情或配置后，运行 `npm run validate-game -- games/<id>`。
- 修改运行素材后，确认文件存在、透明通道与尺寸符合要求，并在浏览器检查实际合成效果。
- 公共框架改动必须回归所有已登记游戏；一部游戏通过不能代表框架通过。
